import React from "react";
import TodoList from "./todolist"; // Đảm bảo rằng tệp todolist.js xuất thành phần TodoList

function App() {
  return (
    <TodoList />
  );
}

export default App; // Xuất thành phần mà không gọi hàm
